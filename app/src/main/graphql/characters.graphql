query Character($characterId: ID!) {
    character(id: $characterId) {
        name
        status
        image
        created
    }
}

query Characters {
    characters {
        results {
            name
            status
            gender
            image
            created
        }
    }
}